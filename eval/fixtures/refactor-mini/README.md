# refactor-mini

A tiny service config used by the d2c eval corpus.

## Behavior

The client waits up to a request timeout of 30 seconds before giving up.
Change the timeout in `config.py`, this README, and `tests/test_config.py`
together.
