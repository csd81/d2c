# Guide

This guide explains how to use the sample tool.

## Installation

Run `pip install sample-tool` to install it.

## Usage

Once installed, run `sample-tool --help` to see the available commands.
When the tool starts, it will recieve input from stdin.

## Support

If you have questions, open an issue.
