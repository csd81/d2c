"""d2c — a Python re-implementation of the Claude Code agent architecture."""

# Single source of truth for the package version (pyproject reads this via
# [tool.setuptools.dynamic]).
__version__ = "0.1.0"
