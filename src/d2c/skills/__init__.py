"""Skills — domain-specific instructions injected into context at invocation time.

Paper Section 6.1: Skills are the low-context-cost extensibility mechanism.
Only the description is in the system prompt; the full prompt loads on invocation.
"""
